<div class="tab attributes {{tab.cssClass}}" data-group="primary" data-tab="attributes">
  <div class="standard-attributes">
    <fieldset class="attribute-set">
      <legend>{{localize "UTOPIA.Item.Actions.attributes"}}</legend>
      
      <div class="form-group slim">
        <label for="system.formula">{{localize "UTOPIA.CommonTerms.formula"}}</label>
        {{formInput systemFields.formula name="system.formula" value=system.formula localize=true}}
      </div>

      <div class="form-group slim">
        <label for="system.type">{{localize "UTOPIA.Item.Actions.type"}}</label>
        {{formInput systemFields.type name="system.type" value=system.type localize=true}}
      </div>

      <div class="form-group slim">
        <label for="system.cost">{{localize "UTOPIA.Item.Actions.cost"}}</label>
        {{formInput systemFields.cost name="system.cost" value=system.cost localize=true}}
      </div>

      <div class="form-group slim">
        <label for="system.stamina">{{localize "UTOPIA.Item.Actions.stamina"}}</label>
        {{formInput systemFields.stamina name="system.stamina" value=system.stamina localize=true}}
      </div>

      <div class="form-group slim">
        <label for="system.category">{{localize "UTOPIA.Item.Actions.category"}}</label>
        {{formInput systemFields.category name="system.category" value=system.category localize=true}}
      </div>
    </fieldset> 
  
    <fieldset class="attribute-set">
      <legend>{{localize "UTOPIA.Item.Actions.resources"}}</legend>

      <div class="form-group slim">
        <label for="system.resource">{{localize "UTOPIA.Item.Actions.resource"}}</label>
        {{formInput systemFields.resource name="system.resource" value=system.resource localize=true}}
      </div>

      <div class="form-group slim">
        <label for="system.consumed">{{localize "UTOPIA.Item.Actions.consumed"}}</label>
        {{formInput systemFields.consumed name="system.consumed" value=system.consumed localize=true}}
      </div>
    </fieldset>

    <fieldset class="attribute-set">
      <legend>{{localize "UTOPIA.Item.Actions.macro"}}</legend>

      <div class="form-group slim">
        <label for="system.macro">{{localize "UTOPIA.Item.Actions.macro"}}</label>
        {{formInput systemFields.macro name="system.macro" value=system.macro localize=true}}
      </div>

      <div class="form-group slim">
        <label for="system.actor">{{localize "UTOPIA.Item.Actions.actor"}}</label>
        {{formInput systemFields.actor name="system.actor" value=system.actor localize=true}}
      </div>

      <div class="form-group slim">
        <label for="system.template">{{localize "UTOPIA.Item.Actions.template"}}</label>
        {{formInput systemFields.template name="system.template" value=system.template localize=true}}
      </div>
    </fieldset>
  </div>
</div>